# ProjectNX 0.2.0-preview
# O client_id e publico, mas deve pertencer a um aplicativo registrado por nos.
# Nunca coloque senha, client_secret, token ou refresh_token neste arquivo.

client_id=
tenant=consumers
